<form method="post" action="{{ path('app_album_photo_delete', {'id': album_photo.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet album ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ album_photo.id) }}">
    <button class="btn btn-danger">Effacer</button>
</form>
